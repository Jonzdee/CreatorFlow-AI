export const POSTING_FREQUENCIES = [
    "Daily",
    "3 Times Weekly",
    "Weekly",
];

export const POSTING_FREQUENCY_OPTIONS =
    POSTING_FREQUENCIES.map((item) => ({
        id: item,
        title: item,
    }));